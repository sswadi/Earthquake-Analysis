# Earthquake Data Visualization

## Introduction

This project aims to visualize earthquake data using a web application. The application fetches earthquake data from an API, stores it in a MongoDB database, and then converts the data into GeoJSON format for visualization on a map. The web application uses Leaflet.js to render the earthquake data on an interactive map.

## Features

- Fetch earthquake data from an external API
- Store earthquake data in a MongoDB database
- Convert earthquake data to GeoJSON format
- Visualize earthquake data on an interactive map using Leaflet.js

## Installation

Before running the application, ensure you have the following dependencies installed:

- Node.js
- MongoDB

To install the required packages, run the following command: npm install

To run the application: npm start


